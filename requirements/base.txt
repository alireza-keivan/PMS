# Core framework
Django==5.2.17
django-ninja==1.3.0          # webhook + external API routes (Beds24, WhatsApp, Stripe)
psycopg[binary]==3.2.3       # PostgreSQL driver

# Background + scheduled jobs
celery==5.4.0
redis==5.2.1
django-celery-beat==2.9.0    # cron-style schedules editable from Django admin

# Outbound HTTP to external APIs
httpx==0.28.1

# Files, images, storage
django-storages[s3]==1.14.4  # S3-compatible object storage (prod)
Pillow==11.1.0               # image processing; WebP output required - see CLAUDE.md

# Calendar feeds (basic sync tier)
icalendar==6.1.0

# Authentication
django-allauth[socialaccount]==65.19.1  # Google sign-in; also the path for phone/OTP later

# Config + utilities
django-environ==0.12.0       # env-var driven settings
django-htmx==1.21.0          # request.htmx helper for partial responses
